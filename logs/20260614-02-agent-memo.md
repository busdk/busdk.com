# 2026-06-14 02:00 EEST Supervisor Memo

## Session Context

The first pass was rejected because it used the wrong checkout. I have now
located the real `busdk.com` repository under
`/Users/test/git/busdk/agent-supervisor/projects/busdk/busdk.com` and created a
fresh feature worktree from that exact repository at
`/Users/test/.codex/worktrees/5996/busdk-com-links`.

The branch is `codex/busdk-doc-link-index`, based on the current local
`develop` tip `0001644`. The worktree is clean before editing. The repo-local
rules are straightforward: keep the change scoped to the website docs, use a
deterministic edit, and verify the result with static scans rather than manual
spot checks.

## Work Narrative

The key discovery is that `busdk.com/docs/` is not a separate repository. It is
just the published static tree inside `busdk.com`, so the repair belongs here
instead of in the outer BusDK superproject. The docs pages still contain many
directory-style links such as `gx/`, `../`, and `modules/`, which means local
`file://` browsing will fall off a directory boundary unless those links are
made explicit.

I have not rewritten anything yet. The next move is a scripted HTML pass over
`docs/**/*.html` that converts only local directory `href` and `src` targets to
`index.html`, while leaving external URLs, fragments, downloads, and asset
links alone. After that I will run the three required checks from the current
docs tree and then commit the docs change on the feature branch.

## Work Update

The scripted pass completed across all 99 HTML pages in the published docs
tree. The changes are broad but mechanical: the same directory-link rule was
applied consistently to the site shell, the landing pages, the blog archive,
and the nested GX/UI docs. The important part now is that the tree no longer
depends on a server-side directory redirect for local browsing.

## Verification Update

The static scan found no remaining local directory `href` or `src` links
without `index.html`. The representative nested pages named in the operator
request resolved correctly when treated as `file://` URLs, and `git diff
--check` passed cleanly. At this stage the only remaining work is promotion:
commit the docs tree, update the outer `busdk.com` pointer, and make sure the
feature branch stays clean.

## Final State

Complete so far: the correct repository and feature worktree are identified,
the task is recorded in `PLAN.md`, and the session is open in the right hourly
memo. Incomplete: the actual rewrite, verification, and commit/pointer update.
Verified: the new worktree starts clean from `0001644`. Not verified: link
correctness in the real checkout. Next: run the deterministic rewrite in this
worktree and prove the nested pages resolve locally.
